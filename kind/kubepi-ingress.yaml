apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kubepi-ingress
spec:
  rules:
  - host: "traefik.anchnet.com"
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: kubepi
            port:
              number: 80